# specs/done/

Shipped specs. Files here are **immutable** — they are the historical record of what was agreed
and proven. Behavior changing later? New spec in `active/`, referencing the old one.
(The Claude Code adapter enforces immutability with a hook; other tools rely on review + this rule.)
