# OS / editors
.DS_Store
Thumbs.db
.idea/
*.swp

# Secrets — never commit these
.env
.env.*
!.env.example

# Logs
*.log

# Claude Code personal settings (shared settings.json IS committed after init)
.claude/settings.local.json

# tui/ build artifact (source is committed, the compiled binary isn't — see tui/README.md)
/tui/ianua-tui
