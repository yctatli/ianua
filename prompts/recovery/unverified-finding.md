# R-05 — "Investigate" Finding (owner: QA)

```
This review finding was triaged as "investigate": <finding>.
DO NOT FIX ANYTHING. Produce a MINIMAL REPRODUCTION: the smallest scenario or failing test that
triggers the issue.
- Repro succeeds → the finding is REAL: leave the failing (red) test in place and report; the fix
  goes through the Developer with normal gates.
- No repro after honest effort → propose closing the finding, with reasoning and what you tried.
PROTECTION: reproduction runs in the QA session — the mind that wrote the code is inclined to
say "couldn't reproduce".
```
